{
  "name": "mark-it-down",
  "version": "1.0.0",
  "description": "A library for transforming markdown into html",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "devDependencies": {
    "@types/chai": "^5.0.0",
    "@types/mocha": "^10.0.8",
    "chai": "^5.1.1",
    "mocha": "^10.7.3",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.1"
  },
  "scripts": {
    "test": "mocha --reporter spec --require ts-node/register 'spec/**/*.test.ts'"
  }
}
